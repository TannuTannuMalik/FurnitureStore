<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:fitsSystemWindows="true">

    <!-- HEADER with RECOZY title and icons -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:gravity="center_vertical"
        android:background="@color/forest_green">

        <ImageView
            android:id="@+id/iconHome"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_home"
            app:tint="@android:color/white"
            android:contentDescription="Home"
            android:padding="8dp" />

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="RECOZY"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center" />
        <ImageView
            android:id="@+id/iconProfile"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_profile"
            app:tint="@android:color/white"
            android:contentDescription="Profile"
            android:padding="8dp" />
        <ImageView
            android:id="@+id/iconCart"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_cart"
            app:tint="@android:color/white"
            android:contentDescription="Cart"
            android:padding="8dp"
            android:layout_marginEnd="12dp" />


    </LinearLayout>

    <!-- Search Results Text below green header -->
    <TextView
        android:id="@+id/titleSearchResults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/headerLayout"
        android:padding="12dp"
        android:textColor="#222222"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Product count below search results -->
    <TextView
        android:id="@+id/textTotalProducts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleSearchResults"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:text="Products: 0"
        android:textColor="#444444"
        android:textSize="14sp" />

    <!-- Sorting spinner below product count -->
    <Spinner
        android:id="@+id/spinnerSort"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textTotalProducts"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp" />

    <!-- Product grid RecyclerView below spinner -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_below="@id/spinnerSort"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp" />

</RelativeLayout>
